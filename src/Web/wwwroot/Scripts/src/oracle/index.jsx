import React from 'react';
import './index.scss';
import Pool from './pool/pool.jsx';

const oracle = (props) => {
    return (
        <div>
            <Pool />
        </div>
    );
};

export default oracle;
