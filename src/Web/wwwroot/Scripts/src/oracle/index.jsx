import React from 'react';
import './index.scss';
import Pool from './pool/pool.jsx';

const oracle = (props) => {
    return (
        <div>
            <h1>Oracle</h1>
            <Pool />
        </div>
    );
};

export default oracle;
